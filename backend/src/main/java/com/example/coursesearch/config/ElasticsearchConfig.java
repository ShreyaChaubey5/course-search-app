package com.example.coursesearch.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class ElasticsearchConfig {
    // Optional if using default host/port (localhost:9200)
}

